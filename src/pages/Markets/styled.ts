import { Box, styled } from "@mui/material";

const MarketsWrapper = styled(Box)(() => ({
  maxWidth: "100%",
  height: "100%",
  maxHeight: "100%",
  display: "flex",
  flexWrap: "wrap",
  flex:1
}));

export default MarketsWrapper;
